@model IEnumerable<ThreeStarsandaSun.Models.Restaurant>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RestoName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RestoAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RestoContactNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RestoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RestoAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RestoContactNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RestaurantID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RestaurantID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RestaurantID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
